(function countingComponent(){
	// var arrayOfCounters = [];
	// var topicDescribed = {score:0};

	// function tallyUp() {
	//   TweenMax.to(topicDescribed, 5, {score:"+=" + thisNumberToCountTo, roundProps:"score", onUpdate:updateCount, ease:Expo.easeOut});
	// }

	// //generalize
	// function updateCount() {
	//   arrayOfCounters[0].innerHTML = topicDescribed.score;
	//   //arrayOfCounters[1].innerHTML = topicDescribed.score;
	//   //arrayOfCounters[2].innerHTML = topicDescribed.score;
	// }
	// if(document.querySelector('.counter')){
	// 	arrayOfCounters = document.querySelectorAll('.counter-component__number');		
	// 	for(let i = 0; i < arrayOfCounters.length; i++){
	// 		var thisNumberToCountTo = arrayOfCounters[i].getAttribute('data-numberToCountTo');
	// 	}
	// 	window.addEventListener('load', tallyUp);
	// }
})();